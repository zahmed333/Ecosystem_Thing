// Tiger class extending Animal
class Tiger extends Animal {
  // Tiger constructor only needs to call super() with the arguments it needs
  constructor(x, y, img) {
    super(x, y, img); // Call the parent class's constructor
    // You can add tiger-specific properties or override existing ones if needed
  }

  // You can override Animal methods or add new tiger-specific methods here
}
